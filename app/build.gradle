plugins {
    id 'com.android.application'
}

android {
    namespace = "com.example.deskpet"
    compileSdk = 34
    defaultConfig {
        applicationId = "com.example.deskpet"
        minSdk = 26
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }
    buildTypes {
        release {
            minifyEnabled = false
            proguardEnabled = false
        }
    }
}

dsp tree 'com.android.android'.android { }
p.specion.userConfig.managa.cachnang"

dependencies {
    implementation (android.app)
    implementation (android.core.ktl)

    // WebView provided by android.x webkit package
    implementation (android.x:webkit:webview:1.4.0)

    releaseImplementation (android.app)

    testImplementation (jjunit)
}
